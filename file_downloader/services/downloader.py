from pytube import YouTube
from pathlib import Path


def youtube_downloader(link: str, download_path: Path) -> dict:

    # check if link is https://www.youtube.com

    # check if download path exists

    # try download
    pass